language: cpp
compiler:
  - clang
  - gcc
os:
  - linux
script: ( mkdir build && cd build && cmake .. && make VERBOSE=1 && make all test )
notifications:
  email:
    on_success: change
    on_failure: change
